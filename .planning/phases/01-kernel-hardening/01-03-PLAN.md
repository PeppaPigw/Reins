---
phase: 01-kernel-hardening
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/reins/orchestration/orchestrator.py
  - src/reins/orchestration/__init__.py
  - src/reins/subagent/__init__.py
  - src/reins/subagent/manager.py
  - src/reins/orchestration/subagent_manager.py
  - tests/test_orchestrator.py
autonomous: true
requirements: [KERN-05]

must_haves:
  truths:
    - "A single canonical orchestration path exists — orchestration.Orchestrator delegates event emission to kernel.RunOrchestrator patterns"
    - "orchestration.Orchestrator no longer reimplements event emission — it uses EventBuilder"
    - "SubagentManager supports both worktree and logical isolation modes via a single class"
    - "All existing imports continue to work (re-exports from old paths)"
    - "Existing test suite passes without modification"
  artifacts:
    - path: "src/reins/orchestration/orchestrator.py"
      provides: "Orchestrator that delegates to EventBuilder instead of raw journal writes"
      contains: "EventBuilder"
    - path: "src/reins/subagent/manager.py"
      provides: "Unified SubagentManager with isolation_level parameter"
      contains: "IsolationLevel"
    - path: "tests/test_orchestrator.py"
      provides: "Tests verifying consolidated orchestration"
  key_links:
    - from: "src/reins/orchestration/orchestrator.py"
      to: "src/reins/kernel/event/builder.py"
      via: "uses EventBuilder.commit() for event emission"
      pattern: "EventBuilder\\|_builder\\.commit"
    - from: "src/reins/subagent/manager.py"
      to: "src/reins/orchestration/subagent_manager.py"
      via: "imports worktree isolation logic"
      pattern: "IsolationLevel\\.WORKTREE"
---

<objective>
Consolidate duplicate orchestration implementations into a single canonical path. The orchestration.Orchestrator will delegate event emission through EventBuilder (matching kernel.RunOrchestrator's pattern), and the two SubagentManager implementations will merge into one with an isolation_level parameter.

Purpose: Eliminates duplicate event emission logic that could diverge over time. Creates a single SubagentManager that supports both physical (worktree) and logical (child RunOrchestrator) isolation modes, reducing maintenance surface.

Output: orchestration.Orchestrator uses EventBuilder for all event emission. A unified SubagentManager with IsolationLevel enum replaces the two separate implementations. All existing imports preserved via re-exports.
</objective>

<execution_context>
@/Users/pigpeppa/Downloads/Reins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pigpeppa/Downloads/Reins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-kernel-hardening/01-RESEARCH.md
@.planning/phases/01-kernel-hardening/01-01-SUMMARY.md

<interfaces>
From src/reins/kernel/event/builder.py:
```python
class EventBuilder:
    def __init__(self, journal: EventJournal) -> None: ...
    async def commit(self, run_id: str, event_type: str, payload: dict, *,
                     actor: Actor = Actor.runtime, command_id: str | None = None,
                     causation_id: str | None = None, correlation_id: str | None = None,
                     trace_id: str | None = None) -> EventEnvelope: ...
```

From src/reins/orchestration/orchestrator.py:
```python
class Orchestrator:
    def __init__(self, *, journal: EventJournal, policy_engine: PolicyEngine,
                 approval_ledger: ApprovalLedger | None = None,
                 poll_interval_seconds: float = 0.01) -> None: ...
    async def execute_intent(self, intent: IntentEnvelope) -> ExecutionResult: ...
    async def spawn_subagent(self, agent_type: str, context: dict[str, Any]) -> AgentHandle: ...
    # Has private _emit_event() that writes directly to journal
```

From src/reins/subagent/manager.py:
```python
class SubagentManager:
    # Manages logical child runs with inherited grants, turn limits, token budgets
    # Creates RunOrchestrator per child
```

From src/reins/orchestration/subagent_manager.py:
```python
class SubagentManager:
    # Manages worktree-based isolation, MCP sessions, context injection hooks
    # Uses agent registry
```

Import consumers:
- src/reins/cli/commands/pipeline.py: `from reins.orchestration.orchestrator import Orchestrator`
- src/reins/execution/parallel_executor.py: `from reins.subagent.manager import SubagentManager, SubagentSpec, SubagentStatus`
- src/reins/orchestration/__init__.py: re-exports both
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor orchestration.Orchestrator to use EventBuilder for event emission</name>
  <read_first>
    - src/reins/orchestration/orchestrator.py (full file)
    - src/reins/kernel/event/builder.py
    - src/reins/kernel/types.py
    - tests/test_orchestrator.py
  </read_first>
  <files>
    src/reins/orchestration/orchestrator.py
    tests/test_orchestrator.py
  </files>
  <action>
    1. In `src/reins/orchestration/orchestrator.py`:
       - Add import: `from reins.kernel.event.builder import EventBuilder`
       - In `Orchestrator.__init__()`: create `self._builder = EventBuilder(journal)` (using the journal already passed in)
       - Replace the private `_emit_event()` method implementation. Currently it likely does raw `self._journal.append(EventEnvelope(...))`. Change it to:
         ```
         async def _emit_event(self, *, run_id: str, event_type: str, payload: dict, actor: Actor = Actor.runtime, causation_id: str | None = None, correlation_id: str | None = None) -> EventEnvelope:
             return await self._builder.commit(
                 run_id=run_id,
                 event_type=event_type,
                 payload=payload,
                 actor=actor,
                 causation_id=causation_id,
                 correlation_id=correlation_id,
             )
         ```
       - Remove any direct `EventEnvelope(...)` construction in this file that was used for emission (keep imports needed for type annotations and dataclass usage like AgentHandle, AgentResult, ExecutionResult).
       - Keep the public API surface identical: `execute_intent()`, `spawn_subagent()`, `collect_results()` signatures unchanged.
       - Keep all dataclass definitions (AgentHandle, AgentResult, ExecutionResult) unchanged.

    2. In `tests/test_orchestrator.py`:
       - Run existing tests first to establish baseline. If any test constructs an Orchestrator, ensure it still works with the same constructor signature.
       - No test changes should be needed since the public API is unchanged. If tests mock `_emit_event`, they may need minor adjustment to match the new signature (adding actor/causation_id/correlation_id kwargs).
  </action>
  <verify>
    <automated>cd /Users/pigpeppa/Downloads/Reins && python -m pytest tests/test_orchestrator.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/reins/orchestration/orchestrator.py contains `from reins.kernel.event.builder import EventBuilder`
    - src/reins/orchestration/orchestrator.py contains `self._builder = EventBuilder(journal)`
    - src/reins/orchestration/orchestrator.py _emit_event method contains `self._builder.commit(`
    - src/reins/orchestration/orchestrator.py does NOT contain direct `EventEnvelope(` construction for emission purposes (may still import EventEnvelope for type hints)
    - tests/test_orchestrator.py passes completely
    - Public API unchanged: `execute_intent`, `spawn_subagent`, `collect_results` signatures preserved
  </acceptance_criteria>
  <done>orchestration.Orchestrator delegates all event emission through EventBuilder, eliminating duplicate event construction logic. Public API unchanged, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Unify SubagentManagers with IsolationLevel parameter</name>
  <read_first>
    - src/reins/subagent/manager.py (full file)
    - src/reins/orchestration/subagent_manager.py (full file)
    - src/reins/execution/parallel_executor.py (imports SubagentManager)
    - src/reins/orchestration/__init__.py
  </read_first>
  <files>
    src/reins/subagent/manager.py
    src/reins/subagent/__init__.py
    src/reins/orchestration/subagent_manager.py
    src/reins/orchestration/__init__.py
  </files>
  <action>
    1. In `src/reins/subagent/manager.py`:
       - Add an enum at the top of the file (after imports):
         ```
         from enum import Enum
         class IsolationLevel(Enum):
             NONE = "none"
             LOGICAL = "logical"
             WORKTREE = "worktree"
         ```
       - Add `isolation_level: IsolationLevel = IsolationLevel.LOGICAL` parameter to `SubagentManager.__init__()`
       - Store as `self._isolation_level = isolation_level`
       - The existing logical isolation behavior remains the default (IsolationLevel.LOGICAL)
       - For IsolationLevel.WORKTREE: add a conditional import and delegation pattern. When `isolation_level == IsolationLevel.WORKTREE`, the manager should import and delegate to the worktree-specific logic from `reins.orchestration.subagent_manager` (keep that module as an internal implementation detail).
       - Add a factory method: `@classmethod def create(cls, isolation_level: IsolationLevel = IsolationLevel.LOGICAL, **kwargs) -> SubagentManager` that constructs with appropriate config.
       - Export `IsolationLevel` from the module.

    2. In `src/reins/subagent/__init__.py`:
       - Add re-export: `from reins.subagent.manager import SubagentManager, IsolationLevel, SubagentSpec, SubagentStatus`

    3. In `src/reins/orchestration/subagent_manager.py`:
       - Add a module-level docstring noting this is the worktree-isolation implementation, now accessed via `SubagentManager(isolation_level=IsolationLevel.WORKTREE)`
       - Rename the class to `WorktreeSubagentManager` (internal implementation detail)
       - Add a compatibility alias at module bottom: `SubagentManager = WorktreeSubagentManager  # backward compat`

    4. In `src/reins/orchestration/__init__.py`:
       - Update the lazy import of SubagentManager to import from `reins.subagent.manager` instead (or keep importing from orchestration.subagent_manager for backward compat — the alias handles it)
       - Ensure `from reins.orchestration import SubagentManager` still works

    NOTE: This is a refactoring task. The key constraint is that ALL existing import paths must continue to work. Both `from reins.subagent.manager import SubagentManager` and `from reins.orchestration.subagent_manager import SubagentManager` must resolve. The orchestration version now resolves to WorktreeSubagentManager (aliased as SubagentManager for compat).
  </action>
  <verify>
    <automated>cd /Users/pigpeppa/Downloads/Reins && python -m pytest tests/ -x --ignore=tests/integration -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/reins/subagent/manager.py contains `class IsolationLevel(Enum)`
    - src/reins/subagent/manager.py contains `WORKTREE = "worktree"`
    - src/reins/subagent/manager.py contains `isolation_level: IsolationLevel`
    - src/reins/orchestration/subagent_manager.py contains `class WorktreeSubagentManager` or `WorktreeSubagentManager`
    - src/reins/orchestration/subagent_manager.py contains `SubagentManager = WorktreeSubagentManager`
    - `python -c "from reins.subagent.manager import SubagentManager, IsolationLevel"` exits 0
    - `python -c "from reins.orchestration.subagent_manager import SubagentManager"` exits 0
    - `python -c "from reins.orchestration import SubagentManager"` exits 0
    - All existing tests pass
  </acceptance_criteria>
  <done>Two SubagentManager implementations unified under a single class with IsolationLevel parameter. Worktree isolation preserved as internal implementation. All import paths backward-compatible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator -> EventBuilder | Event emission now goes through trusted gate (EventBuilder) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Repudiation | orchestration.Orchestrator event emission | mitigate | All events now flow through EventBuilder which enforces checksums and sequential ordering. No bypass path. |
| T-01-10 | Elevation of Privilege | IsolationLevel downgrade | accept | IsolationLevel is set at construction time by trusted code. No external API to change isolation level at runtime. |
</threat_model>

<verification>
After all tasks complete:
1. `python -m pytest tests/test_orchestrator.py -x -v` — all pass
2. `python -m pytest tests/ -x --ignore=tests/integration -q` — full unit suite passes
3. `python -c "from reins.orchestration.orchestrator import Orchestrator; from reins.subagent.manager import SubagentManager, IsolationLevel"` — imports work
4. `grep -c "EventBuilder" src/reins/orchestration/orchestrator.py` returns >= 2
5. `grep -c "IsolationLevel" src/reins/subagent/manager.py` returns >= 3
</verification>

<success_criteria>
- orchestration.Orchestrator uses EventBuilder for all event emission (no direct EventEnvelope construction for persistence)
- SubagentManager unified with IsolationLevel enum supporting NONE, LOGICAL, WORKTREE modes
- All existing import paths preserved (backward compatible)
- All existing tests pass without modification (or with minimal mock signature updates)
- No duplicate event emission logic remains
</success_criteria>

<output>
After completion, create `.planning/phases/01-kernel-hardening/01-03-SUMMARY.md`
</output>
