---
phase: 02-security-policy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/reins/platform/remote_registry.py
  - src/reins/integrations/_http.py
  - src/reins/execution/mcp/transport.py
  - tests/unit/test_tls_validation.py
  - tests/integration/test_policy_enforcement.py
autonomous: true
requirements: [SEC-03, SEC-04]
must_haves:
  truths:
    - "All urlopen calls pass an explicit SSL context with certificate validation"
    - "httpx.AsyncClient is created with verify=True explicitly"
    - "No code path can call ExecutionDispatcher.dispatch() without passing through the policy engine"
    - "A test proves that direct dispatcher access outside the orchestrator is architecturally prevented"
  artifacts:
    - path: "src/reins/platform/remote_registry.py"
      provides: "TLS-validated registry fetches"
      contains: "ssl.create_default_context"
    - path: "src/reins/integrations/_http.py"
      provides: "TLS-validated HTTP helper"
      contains: "ssl.create_default_context"
    - path: "src/reins/execution/mcp/transport.py"
      provides: "TLS-validated MCP transport"
      contains: "verify=True"
    - path: "tests/unit/test_tls_validation.py"
      provides: "TLS validation tests"
      min_lines: 40
    - path: "tests/integration/test_policy_enforcement.py"
      provides: "Policy enforcement end-to-end tests"
      min_lines: 50
  key_links:
    - from: "src/reins/platform/remote_registry.py"
      to: "ssl.create_default_context"
      via: "explicit TLS context passed to urlopen"
      pattern: "context=.*create_default_context"
    - from: "src/reins/kernel/orchestrator.py"
      to: "src/reins/execution/dispatcher.py"
      via: "policy gate in process_proposal before dispatch"
      pattern: "process_proposal"
---

<objective>
Enforce TLS certificate validation on all HTTP fetches and verify policy engine has no bypass vectors.

Purpose: SEC-04 ensures no MITM attacks on registry/template fetches. SEC-03 ensures the policy engine cannot be circumvented — the architectural guarantee that every capability-gated action passes through the policy engine.

Output: Explicit TLS contexts on all urlopen calls, explicit verify=True on httpx client, policy bypass audit test, and TLS validation tests.
</objective>

<execution_context>
@/Users/pigpeppa/Downloads/Reins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pigpeppa/Downloads/Reins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-policy/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit TLS certificate validation to all HTTP clients</name>
  <files>src/reins/platform/remote_registry.py, src/reins/integrations/_http.py, src/reins/execution/mcp/transport.py, tests/unit/test_tls_validation.py</files>
  <read_first>
    - src/reins/platform/remote_registry.py (urlopen on line 134 without SSL context)
    - src/reins/integrations/_http.py (urlopen on line 65 without SSL context)
    - src/reins/execution/mcp/transport.py (httpx.AsyncClient on line 81 without verify=True)
  </read_first>
  <action>
    In src/reins/platform/remote_registry.py:
    1. Add `import ssl` to imports (after existing stdlib imports, before urllib imports).
    2. Add a module-level helper function after the imports:
       ```
       def _tls_context() -> ssl.SSLContext:
           """Create a strict TLS context that validates certificates."""
           ctx = ssl.create_default_context()
           ctx.check_hostname = True
           ctx.verify_mode = ssl.CERT_REQUIRED
           return ctx
       ```
    3. On line 134, change `with urlopen(url) as response:` to `with urlopen(url, context=_tls_context()) as response:`
    4. Remove the `# noqa: S310` comment (the explicit context addresses the security concern).

    In src/reins/integrations/_http.py:
    1. Add `import ssl` to imports (after existing stdlib imports).
    2. Add the same `_tls_context()` helper function after imports.
    3. On line 65, change `with urlopen(request, timeout=timeout_seconds) as response:` to `with urlopen(request, timeout=timeout_seconds, context=_tls_context()) as response:`

    In src/reins/execution/mcp/transport.py:
    1. On line 81, change `self._client = httpx.AsyncClient(timeout=self.timeout)` to `self._client = httpx.AsyncClient(timeout=self.timeout, verify=True)`

    Create tests/unit/test_tls_validation.py:
    - Test that remote_registry._tls_context() returns an SSLContext with check_hostname=True and verify_mode=ssl.CERT_REQUIRED
    - Test that _http._tls_context() returns an SSLContext with check_hostname=True and verify_mode=ssl.CERT_REQUIRED
    - Grep-based test: assert every `urlopen(` call in remote_registry.py includes `context=` parameter (use subprocess to grep)
    - Grep-based test: assert every `urlopen(` call in _http.py includes `context=` parameter
    - Grep-based test: assert `httpx.AsyncClient(` in transport.py includes `verify=True`
    - Test that importing remote_registry and calling _tls_context() does not raise
    - Test that importing _http and calling _tls_context() does not raise
  </action>
  <verify>
    <automated>cd /Users/pigpeppa/Downloads/Reins && python -m pytest tests/unit/test_tls_validation.py -x -q && grep -q "context=_tls_context()" src/reins/platform/remote_registry.py && grep -q "context=_tls_context()" src/reins/integrations/_http.py && grep -q "verify=True" src/reins/execution/mcp/transport.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ssl.create_default_context" src/reins/platform/remote_registry.py` outputs "1"
    - `grep -c "ssl.create_default_context" src/reins/integrations/_http.py` outputs "1"
    - `grep -c "context=_tls_context()" src/reins/platform/remote_registry.py` outputs "1"
    - `grep -c "context=_tls_context()" src/reins/integrations/_http.py` outputs "1"
    - `grep -c "verify=True" src/reins/execution/mcp/transport.py` outputs "1"
    - `grep -c "check_hostname = True" src/reins/platform/remote_registry.py` outputs "1"
    - tests/unit/test_tls_validation.py exists with at least 5 test functions
    - `python -m pytest tests/unit/test_tls_validation.py -x` passes
  </acceptance_criteria>
  <done>All HTTP client code (urlopen in remote_registry.py and _http.py, httpx in transport.py) explicitly validates TLS certificates. The security property is visible and grep-able.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and test policy engine enforcement — no bypass vectors</name>
  <files>tests/integration/test_policy_enforcement.py</files>
  <read_first>
    - src/reins/kernel/orchestrator.py (process_proposal method — the policy gate)
    - src/reins/execution/dispatcher.py (ExecutionDispatcher class)
    - src/reins/api/registry.py (how registry creates orchestrators and dispatchers)
    - tests/integration/test_policy_engine.py (existing policy tests to understand patterns)
  </read_first>
  <action>
    Create tests/integration/test_policy_enforcement.py (extend or replace existing if partial):

    The test file must verify SEC-03: no execution-layer bypass vectors.

    Test 1 — "test_dispatcher_only_called_through_orchestrator":
    - Use subprocess to grep the entire src/reins/ directory for files that import ExecutionDispatcher
    - Assert that only these files import it: execution/dispatcher.py itself, execution/__init__.py (re-export), api/registry.py (construction), kernel/orchestrator.py (usage)
    - Any other file importing ExecutionDispatcher is a bypass vector

    Test 2 — "test_dispatcher_dispatch_only_called_from_orchestrator":
    - Grep for `.dispatch(` calls across src/reins/ (excluding tests and __pycache__)
    - Assert the only file calling `.dispatch(` is kernel/orchestrator.py
    - This proves no code path bypasses the policy gate

    Test 3 — "test_process_proposal_calls_policy_before_dispatch":
    - Create a RunOrchestrator with a mock policy engine and mock dispatcher
    - Submit a command proposal
    - Assert the policy engine's evaluate method was called BEFORE dispatcher.dispatch
    - Use mock call_args_list ordering to verify sequence

    Test 4 — "test_policy_deny_prevents_dispatch":
    - Create a RunOrchestrator with a policy engine that always denies
    - Submit a command proposal
    - Assert dispatcher.dispatch was NEVER called
    - Assert the result indicates policy denial

    Test 5 — "test_no_shell_true_in_non_hook_code":
    - Grep for "shell=True" in src/reins/ excluding config/hooks.py
    - Assert count is 0 (hooks are the only accepted shell=True usage)
  </action>
  <verify>
    <automated>cd /Users/pigpeppa/Downloads/Reins && python -m pytest tests/integration/test_policy_enforcement.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_policy_enforcement.py exists with at least 5 test functions
    - `python -m pytest tests/integration/test_policy_enforcement.py -x` passes
    - Test names include: test_dispatcher_only_called_through_orchestrator, test_dispatcher_dispatch_only_called_from_orchestrator, test_process_proposal_calls_policy_before_dispatch, test_policy_deny_prevents_dispatch, test_no_shell_true_in_non_hook_code
    - `grep -c "def test_" tests/integration/test_policy_enforcement.py` outputs at least "5"
  </acceptance_criteria>
  <done>Policy enforcement is verified end-to-end: no code path can reach the dispatcher without passing through the policy engine. Architectural grep tests catch future regressions. The only accepted shell=True usage is in config/hooks.py (user-configured commands).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network -> Registry Fetch | Remote URLs fetched for spec assets could be MITM'd |
| Network -> MCP Transport | JSON-RPC calls to MCP servers traverse network |
| Agent -> Orchestrator -> Dispatcher | Untrusted agent proposals must pass policy before execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Spoofing | remote_registry.py urlopen | mitigate | Explicit ssl.create_default_context() with check_hostname=True, verify_mode=CERT_REQUIRED |
| T-02-06 | Spoofing | _http.py urlopen | mitigate | Same explicit TLS context pattern |
| T-02-07 | Spoofing | transport.py httpx client | mitigate | Explicit verify=True on AsyncClient construction |
| T-02-08 | Elevation of Privilege | ExecutionDispatcher bypass | mitigate | Architectural enforcement — grep tests verify dispatcher only reachable through orchestrator |
| T-02-09 | Tampering | Policy engine circumvention | mitigate | Integration test verifies policy evaluate() called before dispatch() |
</threat_model>

<verification>
After both tasks complete:
1. `python -m pytest tests/unit/test_tls_validation.py tests/integration/test_policy_enforcement.py -x` passes
2. `grep -c "context=_tls_context()" src/reins/platform/remote_registry.py` returns 1
3. `grep -c "verify=True" src/reins/execution/mcp/transport.py` returns 1
4. No file outside orchestrator.py calls .dispatch() on the ExecutionDispatcher
</verification>

<success_criteria>
- All HTTP fetches use explicit TLS certificate validation
- Policy engine enforcement is proven with no bypass vectors
- Architectural grep tests prevent future regressions
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-policy/02-02-SUMMARY.md`
</output>
