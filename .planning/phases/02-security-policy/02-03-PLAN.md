---
phase: 02-security-policy
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/reins/api/models.py
  - src/reins/api/routes.py
  - src/reins/api/command_routes.py
  - tests/unit/test_input_validation.py
  - docs/security/threat-model.md
autonomous: true
requirements: [SEC-05, SEC-06]
must_haves:
  truths:
    - "Malformed API payloads produce structured validation errors with error code and details"
    - "API routes use pydantic models for request body validation"
    - "A formal threat model document exists covering sandboxing, exfiltration, and privilege escalation"
    - "Invalid JSON bodies return 400 with structured error response"
    - "Missing required fields return 400 with field-level error details"
  artifacts:
    - path: "src/reins/api/models.py"
      provides: "Pydantic request/response models for all API endpoints"
      exports: ["CreateRunRequest", "SubmitCommandRequest", "ApprovalRequest", "RejectionRequest", "ErrorResponse"]
      min_lines: 60
    - path: "src/reins/api/routes.py"
      provides: "Routes using pydantic validation"
      contains: "CreateRunRequest"
    - path: "src/reins/api/command_routes.py"
      provides: "Command routes using pydantic validation"
      contains: "SubmitCommandRequest"
    - path: "tests/unit/test_input_validation.py"
      provides: "Input validation tests"
      min_lines: 80
    - path: "docs/security/threat-model.md"
      provides: "Formal STRIDE threat model"
      min_lines: 100
  key_links:
    - from: "src/reins/api/routes.py"
      to: "src/reins/api/models.py"
      via: "import and validate request bodies"
      pattern: "from reins.api.models import"
    - from: "src/reins/api/command_routes.py"
      to: "src/reins/api/models.py"
      via: "import and validate request bodies"
      pattern: "from reins.api.models import"
---

<objective>
Add pydantic input validation to all API endpoints and create the formal threat model document.

Purpose: SEC-06 ensures malformed input produces structured errors instead of crashes or undefined behavior. SEC-05 provides the formal threat model that documents all security decisions, accepted risks, and mitigations for the entire system.

Output: Pydantic request models for all API endpoints, updated route handlers that validate through these models, comprehensive tests for validation behavior, and a STRIDE-based threat model document.
</objective>

<execution_context>
@/Users/pigpeppa/Downloads/Reins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pigpeppa/Downloads/Reins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-security-policy/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pydantic API models and wire into route handlers</name>
  <files>src/reins/api/models.py, src/reins/api/routes.py, src/reins/api/command_routes.py, tests/unit/test_input_validation.py</files>
  <read_first>
    - src/reins/api/routes.py (current route handlers with manual dict.get validation)
    - src/reins/api/command_routes.py (current command routes with manual validation)
    - src/reins/api/server.py (app structure for understanding route registration)
    - src/reins/policy/capabilities.py (for CAPABILITY_RISK_TIERS reference if available)
  </read_first>
  <action>
    Create src/reins/api/models.py with pydantic v2 models:

    1. CreateRunRequest(BaseModel):
       - objective: str = Field(..., min_length=1, max_length=10000)
       - issuer: str = Field(default="user")
       - constraints: list[str] = Field(default_factory=list, max_length=50)
       - requested_capabilities: list[str] = Field(default_factory=list, max_length=100)
       - field_validator on issuer: must be one of {"user", "scheduler", "webhook", "remote_agent"}

    2. SubmitCommandRequest(BaseModel):
       - kind: str = Field(..., min_length=1, max_length=200)
       - args: dict = Field(default_factory=dict)
       - source: str = Field(default="model")
       - rationale_ref: str | None = None
       - idempotency_key: str | None = None
       - evaluate: bool = False
       - field_validator on source: must be one of {"model", "human", "hook", "skill"}

    3. ApprovalRequest(BaseModel):
       - request_id: str = Field(..., min_length=1)
       - granted_by: str = Field(default="human", min_length=1, max_length=200)

    4. RejectionRequest(BaseModel):
       - request_id: str = Field(..., min_length=1)
       - reason: str = Field(default="rejected by human", max_length=2000)
       - rejected_by: str = Field(default="human", min_length=1, max_length=200)

    5. ErrorResponse(BaseModel):
       - error: str
       - code: str = "VALIDATION_ERROR"
       - details: dict | None = None

    Update src/reins/api/routes.py:
    1. Add import: `from reins.api.models import CreateRunRequest, ErrorResponse`
    2. Add a helper function `_validation_error(exc: ValidationError) -> web.Response` that converts pydantic ValidationError to a structured JSON response with status 422, body: {"error": "Validation failed", "code": "VALIDATION_ERROR", "details": exc.errors()}
    3. In handle_create_run: after parsing JSON body, validate with `CreateRunRequest.model_validate(body)`. Catch `ValidationError` and return `_validation_error(exc)`. Use validated model fields instead of dict.get() calls.

    Update src/reins/api/command_routes.py:
    1. Add import: `from reins.api.models import SubmitCommandRequest, ApprovalRequest, RejectionRequest`
    2. Add import: `from pydantic import ValidationError`
    3. In handle_submit_command: validate body with SubmitCommandRequest.model_validate(body). Catch ValidationError, return structured 422. Use model fields.
    4. In handle_approve: validate body with ApprovalRequest.model_validate(body). Catch ValidationError, return structured 422. Use model fields.
    5. In handle_reject: validate body with RejectionRequest.model_validate(body). Catch ValidationError, return structured 422. Use model fields.

    Also add `from pydantic import ValidationError` to routes.py.

    Create tests/unit/test_input_validation.py:
    - Test CreateRunRequest with valid data succeeds
    - Test CreateRunRequest with empty objective raises ValidationError
    - Test CreateRunRequest with invalid issuer raises ValidationError (issuer="hacker")
    - Test CreateRunRequest with objective exceeding 10000 chars raises ValidationError
    - Test SubmitCommandRequest with valid data succeeds
    - Test SubmitCommandRequest with empty kind raises ValidationError
    - Test SubmitCommandRequest with invalid source raises ValidationError (source="unknown")
    - Test ApprovalRequest with empty request_id raises ValidationError
    - Test RejectionRequest with reason exceeding 2000 chars raises ValidationError
    - Test ErrorResponse serialization produces expected JSON structure
    - Test that ValidationError.errors() produces a list with field-level details (loc, msg, type)
  </action>
  <verify>
    <automated>cd /Users/pigpeppa/Downloads/Reins && python -m pytest tests/unit/test_input_validation.py -x -q && python -c "from reins.api.models import CreateRunRequest, SubmitCommandRequest, ApprovalRequest, RejectionRequest, ErrorResponse; print('imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/reins/api/models.py exists and `python -c "from reins.api.models import CreateRunRequest, SubmitCommandRequest, ApprovalRequest, RejectionRequest, ErrorResponse"` succeeds
    - `grep -c "model_validate" src/reins/api/routes.py` outputs at least "1"
    - `grep -c "model_validate" src/reins/api/command_routes.py` outputs at least "3"
    - `grep -c "ValidationError" src/reins/api/routes.py` outputs at least "1"
    - `grep -c "ValidationError" src/reins/api/command_routes.py` outputs at least "1"
    - `grep -c "VALIDATION_ERROR" src/reins/api/models.py` outputs at least "1"
    - tests/unit/test_input_validation.py exists with at least 9 test functions
    - `python -m pytest tests/unit/test_input_validation.py -x` passes
  </acceptance_criteria>
  <done>All API endpoints validate request bodies through pydantic models. Invalid input returns structured 422 responses with field-level error details. No endpoint accepts unvalidated dict input.</done>
</task>

<task type="auto">
  <name>Task 2: Write formal STRIDE threat model document</name>
  <files>docs/security/threat-model.md</files>
  <read_first>
    - .planning/phases/02-security-policy/02-RESEARCH.md (Security Domain section — STRIDE patterns, ASVS categories, known threats)
    - src/reins/execution/adapters/shell.py (post-fix state — exec-style)
    - src/reins/api/server.py (post-fix state — localhost default)
    - src/reins/config/hooks.py (accepted risk — shell=True for user hooks)
    - src/reins/execution/adapters/fs.py (path containment check — accepted TOCTOU risk)
  </read_first>
  <action>
    Create docs/security/ directory and docs/security/threat-model.md.

    The document must follow STRIDE methodology and cover these sections:

    1. Header: "# Reins Security Threat Model" with date, version (1.0), scope statement.

    2. "## System Overview": Brief description of Reins as an agent control kernel. Reference the architecture diagram from RESEARCH.md (input validation boundary -> orchestrator -> policy engine -> dispatcher -> adapters).

    3. "## Trust Boundaries":
       - TB-1: External Input -> Validation Layer (CLI args, API payloads, config files)
       - TB-2: Agent Proposals -> Policy Engine (untrusted agent commands)
       - TB-3: Orchestrator -> Execution Dispatcher (policy-gated commands)
       - TB-4: Adapters -> External Systems (subprocess, network, filesystem)
       - TB-5: Network -> API Server (HTTP requests from network)

    4. "## STRIDE Analysis" with subsections for each category:
       - Spoofing: TLS validation on registry fetches, MCP transport. Mitigation: explicit ssl.create_default_context().
       - Tampering: Shell injection via subprocess, YAML deserialization, path traversal. Mitigation: exec-style invocation, yaml.safe_load (already used), path containment checks.
       - Repudiation: Event journal provides audit trail. All actions are event-sourced with checksums.
       - Information Disclosure: Network exposure of API, agent exfiltration via network shell. Mitigation: localhost-default binding, sandboxed adapter strips network env vars.
       - Denial of Service: Malformed payloads causing crashes. Mitigation: pydantic validation with structured error responses.
       - Elevation of Privilege: Policy bypass via direct dispatcher access. Mitigation: architectural enforcement — dispatcher only reachable through orchestrator policy gate.

    5. "## Threat Register" table:
       | ID | Category | Component | Threat | Disposition | Mitigation |
       Include all T-02-01 through T-02-09 threats from plans 01 and 02, plus:
       - T-02-10: Tampering / config/hooks.py / User-configured hooks run with shell=True / accept / Hooks are local user commands analogous to git hooks
       - T-02-11: Tampering / fs.py / TOCTOU in path containment / accept / Agent sandboxing context, not adversarial multi-user
       - T-02-12: DoS / API routes / Malformed JSON payloads / mitigate / Pydantic validation returns structured 422 errors
       - T-02-13: Repudiation / kernel / Actions without audit trail / mitigate / Event sourcing with SHA-256 checksums on every EventEnvelope

    6. "## Accepted Risks" section documenting:
       - Hook executor shell=True (rationale: user-configured local commands)
       - TOCTOU in filesystem adapter (rationale: agent sandboxing, not adversarial)
       - No authentication on API (rationale: localhost-only by default, v1 scope)

    7. "## Security Controls Summary" table:
       | Control | Implementation | Requirement |
       - Exec-style subprocess | shell.py, worktree_manager.py | SEC-01
       - Localhost-default binding | server.py | SEC-02
       - Policy engine enforcement | orchestrator.py | SEC-03
       - TLS certificate validation | remote_registry.py, _http.py, transport.py | SEC-04
       - Input validation | api/models.py, config/loader.py | SEC-06

    8. "## Recommendations for Future Phases":
       - Rate limiting on API endpoints
       - Authentication/authorization for non-localhost access
       - Content Security Policy headers
       - Audit log rotation and retention policy
  </action>
  <verify>
    <automated>cd /Users/pigpeppa/Downloads/Reins && test -f docs/security/threat-model.md && grep -c "STRIDE" docs/security/threat-model.md | grep -qv "^0$" && grep -c "Accepted Risks" docs/security/threat-model.md | grep -qv "^0$" && grep -c "SEC-0" docs/security/threat-model.md | grep -qv "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - docs/security/threat-model.md exists
    - `wc -l docs/security/threat-model.md` outputs at least 100 lines
    - `grep -c "STRIDE" docs/security/threat-model.md` outputs at least "1"
    - `grep -c "Trust Boundar" docs/security/threat-model.md` outputs at least "1"
    - `grep -c "Accepted Risk" docs/security/threat-model.md` outputs at least "1"
    - `grep -c "Threat Register" docs/security/threat-model.md` outputs at least "1"
    - `grep -c "SEC-01" docs/security/threat-model.md` outputs at least "1"
    - `grep -c "SEC-02" docs/security/threat-model.md` outputs at least "1"
    - `grep -c "SEC-03" docs/security/threat-model.md` outputs at least "1"
    - `grep -c "SEC-04" docs/security/threat-model.md` outputs at least "1"
    - `grep -c "SEC-06" docs/security/threat-model.md` outputs at least "1"
    - `grep -c "shell=True" docs/security/threat-model.md` outputs at least "1" (documenting accepted risk)
    - `grep -c "create_subprocess_exec" docs/security/threat-model.md` outputs at least "1" (documenting mitigation)
  </acceptance_criteria>
  <done>A formal STRIDE-based threat model exists at docs/security/threat-model.md covering all security boundaries, threats, mitigations, and accepted risks. It references all SEC-XX requirements and documents the security posture of the system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network -> API Routes | Untrusted JSON payloads from HTTP clients |
| Config Files -> Loader | YAML files that could contain unexpected types |
| Documentation | Cross-cutting security documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Denial of Service | API routes | mitigate | Pydantic validation with structured 422 error responses, field-level constraints (max_length, min_length) |
| T-02-11 | Tampering | API routes | mitigate | Type-safe pydantic models prevent type confusion attacks (e.g., passing list where string expected) |
| T-02-12 | Information Disclosure | API error responses | mitigate | Structured ErrorResponse model — no stack traces or internal details leaked to clients |
</threat_model>

<verification>
After both tasks complete:
1. `python -m pytest tests/unit/test_input_validation.py -x` passes
2. `python -c "from reins.api.models import CreateRunRequest"` succeeds
3. `test -f docs/security/threat-model.md` succeeds
4. `grep -c "STRIDE" docs/security/threat-model.md` returns non-zero
5. All API routes use pydantic model_validate for request bodies
</verification>

<success_criteria>
- All API endpoints validate input through pydantic models
- Malformed input returns structured 422 responses (not 500 crashes)
- Formal threat model document exists with STRIDE analysis
- Threat model covers sandboxing, exfiltration, and privilege escalation
- All SEC-XX requirements are referenced in the threat model
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-policy/02-03-SUMMARY.md`
</output>
